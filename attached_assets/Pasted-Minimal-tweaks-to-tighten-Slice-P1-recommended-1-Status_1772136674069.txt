Minimal tweaks to tighten Slice P1 (recommended)
1) Status + naming consistency

Keep status = "draft" only (as you have).

Make sure UI labels say Draft and don’t introduce “finalized/sent” yet.

2) One estimate PDF rule

In proposal_files, enforce: only one active fileType="estimate_pdf" per proposal.

When a new estimate PDF is finalized:

Either automatically delete the previous estimate PDF record + storage object, or

Mark the old one as removed (soft delete) and hide it (I’d do hard delete for Slice P1 simplicity).

Add this to T002/T004 acceptance implicitly.

3) Display order

You already have displayOrder. Decide one of these for Slice P1:

Simplest: set displayOrder = createdAt order and don’t support reordering yet.

If you want predictable PDF/image ordering later, start now:

When uploading images, assign displayOrder = max + 1.

4) File validation guardrails (keep simple)

Add basic validations in the upload flow:

estimate_pdf must be application/pdf

image must be an image mime type

Max file sizes (pick something safe now, you can adjust later):

PDFs: e.g., 25–50MB

Images: e.g., 10–15MB each
This prevents weird failures later.

5) Deleting a proposal should clean storage

You already included this. Just ensure it’s explicit in acceptance:

DELETE /api/proposals/:id removes proposal_files records and deletes the storage objects.

6) Role access: Office can add proposals (✅) but…

Your spec says “Admin and Office roles only” — great.
Just ensure Office can also access the Customer Proposals tab and create drafts there (you already planned it).

7) Customer creation behavior

Your earlier intent was “select or create a new customer first.”
Your current T003 only includes selecting existing customers.
Two options:

Keep Slice P1 simple (recommended): selection only. If they need a new customer, they create it in CRM first.

If you truly want “create customer here,” that’s extra scope and risk; I’d push it to Slice P1.5 or Slice P2.

Given your Slice P1 objective, I’d keep it selection-only.