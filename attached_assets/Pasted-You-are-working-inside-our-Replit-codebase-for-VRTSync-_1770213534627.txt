You are working inside our Replit codebase for VRTSync. I need you to stop summarizing and instead produce the actual “existing ticket type workflows” in a way that is correct and verifiable.

Context / Problem
- The UI shows 8 “Work Types” (billing categories) in the picker.
- The database currently has only 3 “Ticket Types” (workflow templates): Project, Invoice, To-Do.
- A “RFP Request” workflow (18 steps) exists in code but is not present in the database.
- Work Types and Ticket Types are different: Work Types are billing categories; Ticket Types define the workflow statuses/steps for tickets.
- We need the complete list of Ticket Types AND their full workflows from the system (database and/or code), plus how Work Types map to Ticket Types. We also need to fix the mismatch so every Work Type resolves to a valid Ticket Type + workflow.

Your Tasks (do these in order, with evidence)

1) Identify source of truth for workflows
- Determine whether workflow templates (ticket types + steps/statuses) are intended to be stored in:
  A) database tables, or
  B) code config/constants, or
  C) mixed (code seeds DB).
- Show the exact files / modules and exact tables involved.

2) Inventory current DB state (must be exact)
- List all Ticket Types currently in the database (name + id + any key fields).
- For each Ticket Type, list all workflow steps/statuses (in order) from the database, including:
  - step index/order
  - status key / internal code
  - display label
  - terminal statuses (closed/done)
  - any flags that impact behavior (billable, requires QB, etc) if present
- List all Work Types currently in the database (name + id + billing flags/fields).
- Show how Work Types map to Ticket Types today:
  - If mapping is via foreign key, show the FK field.
  - If mapping is via code logic (e.g., switch/case), show the code path.
  - Output a table: WorkType → TicketType → billing behavior.

3) Inventory code-defined workflows (must be exact)
- Search the codebase for any workflow definitions (e.g., “RFP Request” 18-step flow).
- For every workflow found in code:
  - provide the file path and the exported object/structure
  - output the steps/statuses in order (same fields as DB inventory)
  - confirm whether it is referenced anywhere in runtime (routes/services/UI).

4) Reconcile mismatch and propose the correct fix
- Explain why we have 8 Work Types but only 3 Ticket Types in DB.
- Decide the correct architecture moving forward:
  Option A (preferred): DB-driven workflows as source of truth, with a seed/migration from code if needed.
  Option B: code-driven workflows with DB only storing current status.
- Recommend ONE option and justify based on maintainability and admin editability.
- Then implement (or outline exact PR-ready changes) to make the system consistent.

5) Implement DB-driven workflows (if Option A)
- Create/extend a proper schema if missing:
  - ticket_types table
  - ticket_type_statuses/steps table (or a JSON column with ordering) with constraints
  - work_types table (already exists) mapping to ticket_types via FK
- Write a migration or seed script that:
  - inserts missing Ticket Types (including “RFP Request” and any others found in code)
  - inserts their workflow steps/statuses in correct order
  - updates Work Types to point to the correct Ticket Type
- Ensure idempotency (safe to run multiple times).
- Add guardrails:
  - Prevent creating a ticket with a Work Type that has no Ticket Type mapping.
  - Prevent setting a ticket to a status not in its Ticket Type workflow.

6) Validate end-to-end
- Show that the API returns:
  - all Ticket Types
  - each Ticket Type’s workflow steps
  - Work Types and their mapped Ticket Types
- Verify the UI:
  - Work Type picker remains the same
  - Selecting each Work Type results in a ticket with the correct Ticket Type and workflow statuses available
- Add at least one basic test (or runtime assertion) proving a Work Type cannot map to a nonexistent Ticket Type.

Deliverables I want back in your response
- A clean, final “System of Record” output:
  1) Ticket Types list (all)
  2) Workflows per Ticket Type (ordered steps/statuses)
  3) Work Type → Ticket Type mapping table
- The exact file paths + code snippets you changed or created (migration/seed + mapping logic).
- The exact commands to run migration/seed in Replit to apply the fix.

Important constraints
- Do NOT guess statuses or steps. Everything must come from DB and/or existing code definitions.
- If something doesn’t exist, explicitly say it does not exist and create it as part of the fix (Option A).
- Keep naming consistent with the UI where possible.
- The end state must ensure every Work Type resolves to a valid Ticket Type workflow.

Start now by printing:
(1) the DB schema objects involved (table names + key columns),
(2) the current rows for Ticket Types and Work Types,
(3) the locations in code where workflows are defined and where mapping occurs.
